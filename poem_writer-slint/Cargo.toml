[package]
name = "poem_writer"
version = "1.0.0"
edition = "2021"
authors = ["Your Name <you@example.com>"]
description = "Poem writing app with Intention Space physics (Rust + Slint)"

[dependencies]
slint = "1.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
slint-build = "1.8"
